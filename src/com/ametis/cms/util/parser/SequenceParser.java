/**
 * 
 */
package com.ametis.cms.util.parser;


/**
 * @author Mashuri Hasan
 *
 */
public class SequenceParser {
	
	public void refreshSequence() {
	    /*ConfigurationService configurationService = new ConfigurationService();

	    List<Configuration> configurationList = configurationService.getAll();

	    DDLService service = new DDLService();

	    for(Configuration item : configurationList) {
	      if(item.getBatchNumberSeqName() != null) {
	        createSequence(item.getBatchNumberSeqName().trim(), service);
	      }
	      if(item.getCallNumberSeqName() != null) {      
	        createSequence(item.getCallNumberSeqName().trim(), service);
	      }
	      if(item.getCaseNumberSeqName() != null) {
	        createSequence(item.getCaseNumberSeqName().trim(), service);
	      }
	      if(item.getCaseRejectSeqName() != null) {
	        createSequence(item.getCaseRejectSeqName().trim(), service);
	      }
	      if(item.getCdvNumberSeqName() != null) {
	        createSequence(item.getCdvNumberSeqName().trim(), service);
	      }
	      if(item.getClaimNumberSeqName() != null) {
	        createSequence(item.getClaimNumberSeqName().trim(), service);
	      }
	      if(item.getClaimPendingSeqName() != null) {
	        createSequence(item.getClaimPendingSeqName().trim(), service);
	      }
	      if(item.getClaimRejectSeqName() != null) {
	        createSequence(item.getClaimRejectSeqName().trim(), service);
	      }
	      if(item.getExcessNumberSeqName() != null) {
	        createSequence(item.getExcessNumberSeqName().trim(), service);
	      }
	      if(item.getFundNumberSeqName() != null) {
	        createSequence(item.getFundNumberSeqName().trim(), service);
	      }
	      if(item.getInvoiceNumberSeqName() != null) {
	        createSequence(item.getInvoiceNumberSeqName().trim(), service);
	      }
	      if(item.getOtherPaymentNumSeqName() != null) {
	        createSequence(item.getOtherPaymentNumSeqName().trim(), service);
	      }
	      if(item.getCardNumberSeqName() != null) {
	        if(item.getCardNumberSeqName() != null || !item.getCardNumberSeqName().equalsIgnoreCase("")) {
	          service.createSequence(item.getCardNumberSeqName());
	        }
	      }
	    }*/
	  }

	 /* public void createSequence(String raw, DDLService service) {
	    if(raw != null || !raw.equalsIgnoreCase("")) {
	      service.createSequence(raw.split("'")[1]);
	    }
	  }*/
}
